apiVersion: v1
kind: Service
metadata:
  name: kuard-svc
  namespace: victor-leopold
spec:
  selector:
    app: frontend
    soft: kuard
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
